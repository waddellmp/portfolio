/** @type {import('next').NextConfig} */
const nextConfig = {
 reactStrictMode: true,
 assetPrefix: '/portfolio',
 basePath: '/portfolio',
 images: {
  unoptimized: true,
 },
};

module.exports = nextConfig;
